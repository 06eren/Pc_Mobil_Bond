<Window x:Class="PCIstemci.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PCIstemci"
        mc:Ignorable="d"
        Title="PC İstemci" Height="700" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#1C1C1E">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0A84FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3399FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2C2C2E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3A3A3C"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#2C2C2E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3A3A3C"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#AEAEB2"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="#0A84FF"/>
            <Setter Property="BorderBrush" Value="#3A3A3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="ModernListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" BorderThickness="1" BorderBrush="Transparent" Background="#2C2C2E" CornerRadius="10" Padding="15">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="BorderBrush" TargetName="Bd" Value="#0A84FF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="Bd" Value="#3A3A3C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>


        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Label Content="Ağdaki Cihazlar" FontSize="22" FontWeight="Bold" Foreground="White" Margin="5,0,0,10"/>
                <Button x:Name="RefreshButton" Content="Yenile" Click="RefreshButton_Click" Padding="8,5" Height="30" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>

            <ListView Grid.Row="1" x:Name="DeviceListView" Background="Transparent" BorderThickness="0" Foreground="White" SelectionChanged="DeviceListView_SelectionChanged" ItemContainerStyle="{StaticResource ModernListViewItemStyle}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" Foreground="White"/>
                            <TextBlock Text="{Binding IpAddress}" Foreground="#AEAEB2" FontSize="12"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Label Grid.Row="2" Content="Kayıtlı Cihazlar" FontSize="18" FontWeight="Bold" Foreground="White" Margin="5,20,0,10"/>
            <ListView Grid.Row="3" x:Name="PairedDeviceListView" Background="Transparent" BorderThickness="0" Foreground="White" SelectionChanged="PairedDeviceListView_SelectionChanged" ItemContainerStyle="{StaticResource ModernListViewItemStyle}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" Foreground="White"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>


        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <GroupBox Header="Bağlantı">
                    <StackPanel>
                        <Label Content="Güvenlik PIN Kodu:"/>
                        <PasswordBox x:Name="PinPasswordBox"/>
                        <Button x:Name="ConnectButton" Content="Bağlan" Click="ConnectButton_Click" Margin="0,15,0,0" Padding="10"/>
                    </StackPanel>
                </GroupBox>
                <Expander Header="Manuel Bağlantı" Foreground="#AEAEB2" Margin="0,15,0,0">
                    <Border Background="#2C2C2E" CornerRadius="8" Padding="10" Margin="0,5,0,0">
                        <StackPanel>
                            <Label Content="Telefonun IP Adresi:"/>
                            <TextBox x:Name="IpAddressTextBox" Text="192.168.1."/>
                        </StackPanel>
                    </Border>
                </Expander>
                <Label x:Name="StatusLabel" Content="Cihazlar aranıyor..." HorizontalAlignment="Center" Margin="0,15,0,10" FontStyle="Italic" Foreground="Gray"/>
            </StackPanel>
            <GroupBox Header="Loglar" Grid.Row="1" Margin="0,10,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="LogTextBlock" TextWrapping="Wrap" Background="#2C2C2E" Foreground="#AEAEB2" Padding="5"/>
                </ScrollViewer>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
